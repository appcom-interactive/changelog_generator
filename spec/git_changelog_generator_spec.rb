require 'spec_helper'

describe GitChangelogGenerator do
  it 'has a version number' do
    expect(GitChangelogGenerator::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
